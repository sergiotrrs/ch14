let arr = ["This", "is", "a", "sentence."];

function printOutString(a,b,c,d) {
  let union = a + b + c + d;
  console.log (`${a} ${b} ${c} ${d}`);
}

union = printOutString(arr[0], arr[1], arr[2], arr[3]);