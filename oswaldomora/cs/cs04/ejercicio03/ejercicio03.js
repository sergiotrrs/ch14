let array = [1, 2, 3, 4];
let s = 0;
let m = 1;

for (let i = 0; i < array.length; i += 1) {
    s += array[i];
    m *= array[i];
};
console.log("La suma del arreglo es " + s + " y el producto es: " + p);