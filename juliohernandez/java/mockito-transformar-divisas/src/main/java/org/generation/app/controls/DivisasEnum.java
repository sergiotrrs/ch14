package org.generation.app.controls;

public enum DivisasEnum {
	MXN, 
	CAD,
	EURO
	
	//Con controles de tipo de enum, se le da mayor claridad al código. 
	//Se ahorran los if's
	//Evita que se hagan errores cuando entradas
	
}
