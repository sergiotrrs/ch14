//Intrucciones: Copy the code below into your workspace:
let arr = ["This", "is", "a", "sentence."];

function printOutString( arreglo ) {
    // Your code here
    // El metodo join () une todos los elementos de una arreglo en una cadena
    console.log( arreglo.join(' ') ); 
    alert(arreglo.join(' '));  
}
printOutString(arr);